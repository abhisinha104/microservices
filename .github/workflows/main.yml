name: My GitHub Actions Workflow

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy-ServiceRegistry:
    if: ${{ startsWith(github.event.ref, 'refs/heads/main') && github.event.repository.owner.login == 'abhisinha_104' && github.event.repository.name == 'microservices' && contains(github.event.head_commit.modified, 'microservice1/') }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and push Docker image for microservice1
        run: |
          echo Build started on $(date)
          TAG="image_$(date +'%Y%m%d_%H%M%S')"
          echo Building the Docker image with tag $TAG...
          docker build -t dev-microservices:$TAG ./microservice1
